# Secrets
.env

# Python
.venv/
__pycache__/
*.pyc
*.pyo
.python-version

# Phase 1 intermediate artifact — regenerated every run, not needed in history
data/articles.json

# Persist everything else in data/ (state.md, latest_report.json, history/)
